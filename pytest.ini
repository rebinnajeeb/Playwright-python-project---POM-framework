[pytest]
# Register your "tags" (markers) here so pytest knows them and doesn't warn.
# Run a group with:  pytest -m smoke
markers =
    smoke: quick critical-path checks (fast feedback)
    regression: full regression suite (deeper, data-driven)
    sanity: lightweight sanity checks
    login: tests for the login functionality
    newcars: tests for the new-cars functionality
